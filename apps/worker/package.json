{
  "name": "@dms/worker",
  "version": "0.0.1",
  "private": true,
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev --local",
    "build": "tsc",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute DMS_DB --local --file=src/db/schema.sql",
    "db:migrate:remote": "wrangler d1 execute DMS_DB --remote --file=src/db/schema.sql"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "@hono/zod-validator": "^0.2.2",
    "zod": "^3.23.8",
    "jose": "^5.6.3",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "typescript": "^5.4.5",
    "wrangler": "^3.60.0"
  }
}
